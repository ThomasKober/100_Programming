<Window x:Class="BLE_Interface.Views.DeviceScanView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BLE Device Scan" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Button Content="Scan" Command="{Binding ScanCommand}" Margin="10"/>
        <ListView Grid.Row="1" ItemsSource="{Binding Devices}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}"/>
                    <GridViewColumn Header="RSSI" DisplayMemberBinding="{Binding Rssi}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>